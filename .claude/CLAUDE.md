# graphify
- **graphify** (`.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, use the installed graphify skill or instructions before doing anything else.

# Design frontend
Pour toute tâche de design/UI/frontend sur ce projet (nouvelle interface, refonte visuelle, page vitrine, composant, palette, typographie, etc.), utiliser systématiquement les skills `frontend-design:frontend-design` et `ui-ux-pro-max:*` (plugins installés) avant d'écrire du HTML/CSS, sauf si l'utilisateur demande explicitement de les ignorer.
